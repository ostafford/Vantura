<div class="min-h-screen bg-gray-50 dark:bg-gray-900">
  <!-- Header -->
  <div class="bg-white dark:bg-gray-800 shadow-sm border-b border-gray-200 dark:border-gray-700">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-3xl font-bold text-gray-900 dark:text-white">Vantura</h1>
          <p class="text-sm text-gray-600 dark:text-gray-400 mt-1">Your cash flow projection tool</p>
        </div>

        <% if @account %>
          <div class="flex items-center gap-4">
            <div class="text-right">
              <p class="text-sm text-gray-600 dark:text-gray-400">Last synced</p>
              <p class="text-sm font-medium text-gray-900 dark:text-white">
                <%= @account.last_synced_at ? time_ago_in_words(@account.last_synced_at) + " ago" : "Never" %>
              </p>
            </div>
            
            <!-- Theme Toggle -->
            <button onclick="toggleTheme()" id="themeToggle" class="inline-flex items-center px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors shadow-sm">
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
              </svg>
              <span>Auto</span>
            </button>
            
            <%= button_to sync_path, method: :post, id: "syncButton", class: "inline-flex items-center px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors shadow-sm", data: { disable_with: "Syncing..." } do %>
              <svg class="w-5 h-5 mr-2" id="syncIcon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
              </svg>
              <span id="syncButtonText">Sync Now</span>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Flash Messages -->
  <% if notice || alert %>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <% if notice %>
        <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-4">
          <div class="flex items-center">
            <svg class="w-5 h-5 text-green-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <p class="text-green-800 font-medium"><%= notice %></p>
          </div>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-4">
          <div class="flex items-center">
            <svg class="w-5 h-5 text-red-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <p class="text-red-800 font-medium"><%= alert %></p>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
    <div class="flex gap-3 justify-center">
      <%= link_to calendar_path, class: "inline-flex items-center whitespace-nowrap px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors" do %>
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
        </svg>
        View Calendar
      <% end %>
      
      <% if @account %>
        <%= link_to trends_path, class: "inline-flex items-center whitespace-nowrap px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors" do %>
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
          Trends
        <% end %>
        
        <%= link_to recurring_transactions_path, class: "inline-flex items-center whitespace-nowrap px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors" do %>
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
          </svg>
          Manage Recurring
        <% end %>
        
        <button onclick="openTransactionModal()" class="inline-flex items-center whitespace-nowrap px-4 py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          </svg>
          Add Transaction
        </button>
      <% end %>
    </div>
  </div>


  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
    <% if @account %>
      <!-- Current Date Display -->
      <% if @current_date %>
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-6 mb-4 text-center">
          <p class="text-3xl font-bold text-gray-900 dark:text-white">
            <%= @current_date.strftime("%B %d, %Y") %>
          </p>
          <p class="text-sm text-gray-500 dark:text-gray-400 mt-2">
            <%= @current_date.strftime("%A") %> • Summary for <%= @current_date.strftime("%B %Y") %>
          </p>
        </div>
      <% end %>

      <!-- Account Summary Cards -->
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-4">
        <!-- Current Balance -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm p-6 border border-gray-200 dark:border-gray-700">
          <p class="text-sm font-medium text-gray-600 dark:text-gray-400 mb-1">Current Balance</p>
          <p class="text-3xl font-bold text-blue-600 dark:text-blue-400">
            $<%= number_with_precision(@account.current_balance, precision: 2) %>
          </p>
          <p class="text-xs text-gray-500 dark:text-gray-400 mt-2"><%= @account.display_name %></p>
        </div>

        <!-- End of Month Balance -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm p-6 border border-gray-200 dark:border-gray-700">
          <p class="text-sm font-medium text-gray-600 dark:text-gray-400 mb-1">End of Month</p>
          <p class="text-3xl font-bold <%= @end_of_month_balance >= @account.current_balance ? 'text-green-600 dark:text-green-400' : 'text-orange-600 dark:text-orange-400' %>">
            $<%= number_with_precision(@end_of_month_balance, precision: 2) %>
          </p>
          <p class="text-xs text-gray-500 dark:text-gray-400 mt-2">Projected balance</p>
        </div>

        <!-- Expenses -->
        <%= link_to transactions_expenses_path(year: @current_date.year, month: @current_date.month), class: "bg-white dark:bg-gray-800 rounded-lg shadow-sm p-6 border border-gray-200 dark:border-gray-700 hover:bg-red-50 dark:hover:bg-red-900/20 hover:border-red-300 dark:hover:border-red-700 hover:shadow-md transition-all cursor-pointer" do %>
          <p class="text-sm font-medium text-gray-600 dark:text-gray-400 mb-1">Expenses</p>
          <p class="text-3xl font-bold text-red-600 dark:text-red-400">
            $<%= number_with_precision(@expense_total, precision: 2) %>
          </p>
          <p class="text-xs text-gray-500 dark:text-gray-400 mt-2"><%= @expense_count %> transactions this month</p>
        <% end %>

        <!-- Income -->
        <%= link_to transactions_income_path(year: @current_date.year, month: @current_date.month), class: "bg-white dark:bg-gray-800 rounded-lg shadow-sm p-6 border border-gray-200 dark:border-gray-700 hover:bg-green-50 dark:hover:bg-green-900/20 hover:border-green-300 dark:hover:border-green-700 hover:shadow-md transition-all cursor-pointer" do %>
          <p class="text-sm font-medium text-gray-600 dark:text-gray-400 mb-1">Income</p>
          <p class="text-3xl font-bold text-green-600 dark:text-green-400">
            $<%= number_with_precision(@income_total, precision: 2) %>
          </p>
          <p class="text-xs text-gray-500 dark:text-gray-400 mt-2"><%= @income_count %> transactions this month</p>
        <% end %>
      </div>

      <!-- Recent Transactions -->
      <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700">
        <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700 flex items-center justify-between">
          <h2 class="text-xl font-bold text-gray-900 dark:text-white">Recent Transactions</h2>
          <%= link_to transactions_all_path(year: @current_date.year, month: @current_date.month), 
                      class: "inline-flex items-center px-3 py-1.5 bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 text-sm rounded-lg hover:bg-gray-200 dark:hover:bg-gray-600 transition-colors" do %>
            View All
            <svg class="w-4 h-4 ml-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          <% end %>
        </div>

        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
            <thead class="bg-gray-50 dark:bg-gray-900">
              <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Date</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Description</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Category</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Status</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Amount</th>
              </tr>
            </thead>
            <tbody class="bg-white dark:bg-gray-800 divide-y divide-gray-200 dark:divide-gray-700">
              <% @recent_transactions.each do |transaction| %>
                <tr class="hover:bg-gray-50 dark:hover:bg-gray-700">
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 dark:text-gray-300">
                    <%= transaction.transaction_date.strftime("%b %d, %Y") %>
                  </td>
                  <td class="px-6 py-4 text-sm text-gray-900 dark:text-gray-300">
                    <%= transaction.description %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500 dark:text-gray-400">
                    <%= transaction.category&.humanize || "Uncategorized" %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <div class="flex gap-1">
                      <% if transaction.is_hypothetical %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-purple-100 text-purple-800">
                          Hypothetical
                        </span>
                      <% elsif transaction.status == "HELD" %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                          Pending
                        </span>
                      <% else %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                          Settled
                        </span>
                      <% end %>
                      
                      <% if transaction.recurring? %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                          <svg class="w-3 h-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                          </svg>
                          Recurring
                        </span>
                      <% end %>
                    </div>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-right font-medium <%= transaction.amount < 0 ? 'text-red-600' : 'text-green-600' %>">
                    <%= transaction.amount < 0 ? "-" : "+" %>$<%= number_with_precision(transaction.amount.abs, precision: 2) %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>

    <% else %>
      <!-- No Account Found - Show Sync Instructions -->
      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-12 text-center">
        <h2 class="text-2xl font-bold text-gray-900 mb-4">Welcome to Vantura!</h2>
        <p class="text-gray-600 mb-6">You haven't synced your Up Bank account yet.</p>
        <p class="text-sm text-gray-500 mb-4">Run this in your Rails console to sync your data:</p>
        <code class="bg-gray-100 px-4 py-2 rounded text-sm">UpBank::SyncService.call</code>
      </div>
    <% end %>
  </div>

  <!-- Transaction Modal -->
  <% if @account %>
    <div id="transactionModal" class="hidden fixed inset-0 bg-gray-900 bg-opacity-75 dark:bg-opacity-90 z-50 flex items-center justify-center p-4" style="display: none;">
      <div class="bg-white dark:bg-gray-800 rounded-lg shadow-xl max-w-md w-full">
        <div class="bg-purple-600 dark:bg-purple-700 px-6 py-4 rounded-t-lg">
          <div class="flex items-center justify-between">
            <h3 class="text-xl font-bold text-white">Add Transaction</h3>
            <button onclick="closeTransactionModal()" class="text-white hover:text-gray-200">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
              </svg>
            </button>
          </div>
          <p class="text-purple-100 dark:text-purple-200 text-sm mt-1">Plan future expenses or income</p>
        </div>

        <%= form_with model: Transaction.new, url: transactions_path, method: :post, class: "p-6", id: "transactionForm" do |f| %>
          <div class="space-y-4">
            <!-- Transaction Type -->
            <div>
              <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Transaction Type</label>
              <div class="grid grid-cols-2 gap-3">
                <label class="transaction-type-option cursor-pointer">
                  <%= f.radio_button :transaction_type, 'expense', 
                      checked: true, 
                      class: "hidden transaction-type-radio",
                      onchange: "updateTransactionTypeUI()" %>
                  <div class="transaction-type-card border-2 border-red-500 dark:border-red-600 bg-red-50 dark:bg-red-900/20 rounded-lg p-4 text-center">
                    <svg class="w-8 h-8 mx-auto mb-2 text-red-600 dark:text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"></path>
                    </svg>
                    <span class="font-semibold text-red-700 dark:text-red-400">Expense</span>
                  </div>
                </label>
                
                <label class="transaction-type-option cursor-pointer">
                  <%= f.radio_button :transaction_type, 'income', 
                      class: "hidden transaction-type-radio",
                      onchange: "updateTransactionTypeUI()" %>
                  <div class="transaction-type-card border-2 border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 rounded-lg p-4 text-center hover:border-green-400">
                    <svg class="w-8 h-8 mx-auto mb-2 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <span class="font-semibold text-green-700 dark:text-green-400">Income</span>
                  </div>
                </label>
              </div>
            </div>

            <div>
              <label for="transaction_description" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                <span id="descriptionLabel">What are you buying?</span>
              </label>
              <%= f.text_field :description, 
                  class: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-white rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent",
                  placeholder: "e.g., New laptop, Restaurant dinner",
                  id: "transactionDescription",
                  required: true %>
            </div>

            <div>
              <label for="transaction_amount" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                <span id="amountLabel">How much will it cost?</span>
              </label>
              <div class="relative">
                <span class="absolute left-4 top-2 text-gray-500 dark:text-gray-400 text-lg">$</span>
                <%= f.number_field :amount, 
                    step: 0.01,
                    class: "w-full pl-8 pr-4 py-2 border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-white rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent",
                    placeholder: "0.00",
                    required: true,
                    min: 0.01 %>
              </div>
            </div>

            <div>
              <label for="transaction_transaction_date" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                <span id="dateLabel">When do you plan to buy it?</span>
              </label>
              <%= f.date_field :transaction_date, 
                  value: Date.today,
                  class: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-white rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent",
                  required: true %>
            </div>

            <div>
              <label for="transaction_category" class="block text-sm font-medium text-gray-700 mb-1">
                Category (optional)
              </label>
              <%= f.text_field :category, 
                  class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent",
                  placeholder: "e.g., shopping, entertainment, food" %>
            </div>
          </div>

          <div class="mt-6 flex gap-3">
            <%= f.submit "Add Transaction", 
                id: "submitButton",
                class: "flex-1 bg-purple-600 text-white px-4 py-3 rounded-lg hover:bg-purple-700 transition-colors font-semibold" %>
            <button type="button" onclick="closeTransactionModal()" 
                    class="px-4 py-3 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition-colors font-semibold">
              Cancel
            </button>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>


<script>
  function openTransactionModal() {
    const modal = document.getElementById('transactionModal');
    modal.classList.remove('hidden');
    modal.style.display = 'flex';
    updateTransactionTypeUI(); // Initialize UI state
  }

  function closeTransactionModal() {
    const modal = document.getElementById('transactionModal');
    modal.classList.add('hidden');
    modal.style.display = 'none';
    // Reset form
    document.getElementById('transactionForm').reset();
    updateTransactionTypeUI();
  }

  function updateTransactionTypeUI() {
    const radios = document.querySelectorAll('.transaction-type-radio');
    const cards = document.querySelectorAll('.transaction-type-card');
    let selectedType = 'expense';
    
    radios.forEach((radio, index) => {
      if (radio.checked) {
        selectedType = radio.value;
        cards[index].classList.remove('border-gray-300', 'bg-white');
        if (radio.value === 'expense') {
          cards[index].classList.add('border-red-500', 'bg-red-50');
        } else {
          cards[index].classList.add('border-green-500', 'bg-green-50');
        }
      } else {
        cards[index].classList.remove('border-red-500', 'bg-red-50', 'border-green-500', 'bg-green-50');
        cards[index].classList.add('border-gray-300', 'bg-white');
      }
    });

    // Update labels based on transaction type
    if (selectedType === 'expense') {
      document.getElementById('descriptionLabel').textContent = 'What are you buying?';
      document.getElementById('amountLabel').textContent = 'How much will it cost?';
      document.getElementById('dateLabel').textContent = 'When do you plan to buy it?';
      document.getElementById('transactionDescription').placeholder = 'e.g., New laptop, Restaurant dinner';
    } else {
      document.getElementById('descriptionLabel').textContent = 'What income are you receiving?';
      document.getElementById('amountLabel').textContent = 'How much will you receive?';
      document.getElementById('dateLabel').textContent = 'When do you expect to receive it?';
      document.getElementById('transactionDescription').placeholder = 'e.g., Freelance work, Gift, Tax refund';
    }
  }

  // Close modal on escape key
  document.addEventListener('keydown', function(event) {
    if (event.key === 'Escape') {
      closeTransactionModal();
    }
  });

  // Close modal on background click
  document.getElementById('transactionModal')?.addEventListener('click', function(event) {
    if (event.target === this) {
      closeTransactionModal();
    }
  });

  // Add spinning animation to sync button when clicked
  document.addEventListener('DOMContentLoaded', function() {
    const syncForm = document.querySelector('#syncButton');
    if (syncForm) {
      syncForm.addEventListener('submit', function() {
        const icon = document.getElementById('syncIcon');
        if (icon) {
          icon.classList.add('spinning');
        }
      });
    }
  });
</script>
